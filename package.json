{
  "name": "pdf-merger-root",
  "version": "1.0.0",
  "description": "Professional PDF merger web application with Firebase backend",
  "private": true,
  "workspaces": [
    "web",
    "functions"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web & npm run dev --workspace=functions",
    "build": "npm run build --workspace=web && npm run build --workspace=functions",
    "test": "npm run test --workspace=web && npm run test --workspace=functions",
    "lint": "npm run lint --workspace=web && npm run lint --workspace=functions",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md}\"",
    "firebase:deploy": "firebase deploy"
  },
  "devDependencies": {
    "prettier": "^3.1.0"
  }
}
